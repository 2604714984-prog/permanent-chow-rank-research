# v5 Module 00 — v4 terminal handoff

## H0-01 — Freeze the exact v4 dependency graph

Record the shortest dependency graph from lower 49 to `B1-CLOSED`, the
arbitrary Packet-B global extension, the Packet-A branch closures, and the two
automaticity theorems.  Every node must name a path and exact commit.  Historical
plans are not dependencies.


**Decisive output:** `V4-TERMINAL-LEDGER-FROZEN` with no moving-branch references.

## H0-02 — Separate theorem, route barrier, and diagnostic statuses

Classify every v4 output as `PROVED`, `CLOSED`, `EXACT-SLICE-SURVIVOR`,
`COUNTEREXAMPLE-TO-SHORTCUT`, `ROUTE-EXHAUSTED`, or `DIAGNOSTIC`.  In
particular, the two-term transposition survivor must not be described as a
Packet-B survivor, and automatic `2/5` or `3/4` surjectivity must not be
described as Packet-A progress.


**Decisive output:** A one-page status matrix used by every v5 module.

## H0-03 — Freeze B1 as an imported theorem

Package the exact common-graph proof chain into one theorem-facing dependency
note.  Do not rerun the five Hilbert frontiers.  Record the characteristic-zero,
reduced-point, nonzero-weight, and ordinary-rank scope.


**Decisive output:** `B1-CLOSED-IMPORTED` or a precise audit defect reopening one named lemma.

## H0-04 — Freeze the arbitrary Packet-B intrinsic objects

Record the exact orientations and dimensions of `B`, `C`, the 1645-dimensional
minimal middle, the 1225-rank target composite, and the cubic-apolar
surjectivity map.  Retain the `s=1` and `s=2` rank-six middle types separately.


**Decisive output:** One canonical notation table used by Modules 01–04.

## H0-05 — Freeze the Packet-A active component

Record that the active component has `W^all != 0`, `K2 != 0`, and `K5 != 0`;
that `K2+K5=588`; and that the plain `2/5` and `3/4` induction maps are
automatically surjective.  Mark every superseded opposite-direction statement.


**Decisive output:** One canonical Packet-A frontier statement.

## H0-06 — Inventory all exact survivors and route counterexamples

Freeze coordinates, ranks, and scopes for the one-transposition slice,
two-transposition joins, noncanonical tangent branch, Glynn49 truncation,
non-tensor Sylvester example, and full Glynn64 positive control.  Recompute
only the load-bearing ranks with an independent implementation.


**Decisive output:** A compact survivor/counterexample payload.

## H0-07 — Verify latest CI and evidence integrity

Record the exact workflow run for the frozen input HEAD, including English-only
scan, unit tests, and any independent replay.  A running or failed workflow is
not reported as pass.


**Decisive output:** A CI receipt tied to one exact commit.

## H0-08 — Mark the lower-51/lower-52 grand package dormant

The earlier grand package may remain as a planning record, but its post-lower-50
workstreams must be marked inactive until lower 50 is promoted.  Do not delete
historical files.


**Decisive output:** An explicit dormant marker, not a rewrite of historical content.

## H0-09 — Publish the unique pair of unresolved theorem statements

State the Packet-B termwise-splitting/tail-rigidity theorem and the Packet-A
refined-representation defect theorem in formal mathematical language.  Avoid
task prose in the statements.


**Decisive output:** Two theorem statements that can be independently attacked.

## H0-10 — Authorize v5 execution only after the handoff audit

Reject any v5 computation that uses a superseded orientation, treats the
abstract extension as termwise, or uses a map already proved automatic.


**Decisive output:** `V5-HANDOFF-PASS` or one blocking correction.
