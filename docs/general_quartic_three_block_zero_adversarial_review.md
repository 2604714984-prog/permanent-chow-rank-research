# Adversarial review: quartic three-block zero

The proof uses actual component essential spaces, not full factor spans. Private-polar vanishing follows from the nine-variable floor for nonzero cubic permanent derivatives. Pair-supported polar dimension is computed on the concise essential space of the total form. The disjoint two-term obstruction uses the exact values `F_(8,2)(b)>b` for `1<=b<=8`; it does not assume uniqueness of the explicit sharp-pair construction. No coupled/literal identification or unsupported field transfer is used.

The result proves `mu(8,4)=4` but does not improve unrestricted `ChowRank(perm_8)` or decide `mu(6,4)`.
