# Exact shadow and individual-factor defect budgets inside the `n=6` fixed-four frontier

## Status

`PROOF_DRAFT_COMPLETE` — this note refines `docs/n6_fixed_four_coupled_frontier.md`. Exact rational separators and integer budgets are regenerated by `scripts/n6_fixed_four_coupled_frontier.py`. The equality-plane classification is in `docs/n6_extremal_six_plane_classification.md`, and the former endpoint `b=27` is excluded in `docs/n6_b27_common_quotient_exclusion.md`.

## 1. Exact shadow lower bounds for `20<=b<=27`

Use the fixed-four notation

\[
S
=
\mathcal D_3(\operatorname{perm}_6)
\cap
\mathcal D_3(R),
\qquad
b=\dim S.
\]

The projection argument gives

\[
\dim\partial S\le48.
\]

Bukh's two-dimensional shadow theorem says that, if

\[
b=\binom{x}{3}^2,
\]

then

\[
\dim\partial S\ge\binom{x}{2}^2.
\]

The following rational separators are checked with exact `Fraction` arithmetic.

| `b` | separator `x_0` | `binom(x_0,3)^2<b` | `binom(x_0,2)^2>b+20` | conclusion |
|---:|---:|---:|---:|---:|
| 20 | `41/10` | exact | exact | `dim partial S>=41` |
| 21 | `103/25` | exact | exact | `dim partial S>=42` |
| 22 | `207/50` | exact | exact | `dim partial S>=43` |
| 23 | `104/25` | exact | exact | `dim partial S>=44` |
| 24 | `209/50` | exact | exact | `dim partial S>=45` |
| 25 | `21/5` | exact | exact | `dim partial S>=46` |
| 26 | `211/50` | exact | exact | `dim partial S>=47` |
| 27 | `106/25` | exact | exact | `dim partial S>=48` |

Monotonicity gives

\[
\boxed{
\dim\partial S\ge b+21
\qquad(20\le b\le27).
}
\tag{1.1}
\]

## 2. Defect variables

For the four fixed terms put

\[
G_i=\mathcal D_2(T_i),
\]

and define

\[
\varepsilon_i
=15-\dim G_i,
\qquad
\alpha_i
=3-\dim(E_2\cap G_i).
\tag{2.1}
\]

Both are nonnegative. Apply the omitted-factor projection proof with the `j`th factor hidden. It gives

\[
\dim\partial S
\le
\sum_{i\ne j}\dim G_i
+
\dim(E_2\cap G_j).
\]

Combining this with (1.1) yields:

### Proposition 2.1 — per-omitted-factor defect budget

For every `j=1,2,3,4`,

\[
\boxed{
\sum_{i\ne j}\varepsilon_i+\alpha_j
\le
27-b.
}
\tag{2.2}

Summing the four inequalities gives

\[
\boxed{
3\sum_i\varepsilon_i+\sum_i\alpha_i
\le
4(27-b).
}
\tag{2.3}

## 3. The excluded endpoint `b=27`

At `b=27`, equation (2.2) forces

\[
\varepsilon_i=0,
\qquad
\alpha_i=0
\]

for every `i`. Thus

\[
\dim G_i=15,
\qquad
\dim(E_2\cap G_i)=3,
\qquad
\dim(E_2\cap(G_1+\cdots+G_4))=48.
\]

Equality in every omitted-factor projection has a stronger consequence. A section of

\[
E_2\cap(G_1+\cdots+G_4)
\]

into the external direct sum of the four `G_i` produces, after quotienting by the four three-dimensional intersections, a 36-dimensional length-four linear code whose every triple projection is an isomorphism. The sum-zero condition in `Sym^2 V/E_2` forces the four 12-dimensional quotient images to coincide.

It follows that

\[
G_1\oplus G_2\oplus G_3\oplus G_4
\]

is a direct sum of dimension 60. Differentiating any relation among the four cubic derivative spaces then proves that those spaces are direct as well. Symmetry of the middle catalectics gives coupled central rank

\[
h=4\cdot20=80.
\]

The 19-term residual inequality gives instead

\[
h\le2b-20=34.
\]

Therefore `b=27` is impossible. The current range is

\[
\boxed{20\le b\le26.}
\tag{3.1}
\]

The full proof is in `docs/n6_b27_common_quotient_exclusion.md`.

## 4. The new top layer `b=26`

At `b=26`, equation (2.2) becomes

\[
\boxed{
\sum_{i\ne j}\varepsilon_i+\alpha_j
\le1
\qquad(j=1,2,3,4).
}
\tag{4.1}

The total weighted defect obeys

\[
3\sum_i\varepsilon_i+\sum_i\alpha_i\le4.
\tag{4.2}

These inequalities give an exact defect-pattern classification.

### Proposition 4.1 — the 24 defect patterns

Exactly one of the following holds.

**Family A — one quadratic-dimension defect.** There is a unique index `i` with

\[
\varepsilon_i=1,
\qquad
\varepsilon_j=0\quad(j\ne i).
\]

Then

\[
\alpha_j=0\quad(j\ne i),
\qquad
\alpha_i\in\{0,1\}.
\]

There are eight labelled patterns.

**Family B — no quadratic-dimension defect.** All

\[
\varepsilon_i=0,
\]

and each

\[
\alpha_i\in\{0,1\}
\]

is independent. There are sixteen labelled patterns.

Thus there are precisely

\[
\boxed{8+16=24}
\tag{4.3}
\]

labelled defect patterns.

### Proof

If two distinct `epsilon` variables were positive, choose an omitted index different from both; the corresponding inequality in (4.1) would have left side at least two. Thus at most one `epsilon_i` is positive, and every positive value must be one.

If `epsilon_i=1`, then the inequalities with omitted index `j ne i` contain `epsilon_i+alpha_j`, forcing `alpha_j=0`. The inequality omitting `i` permits `alpha_i<=1`. This is family A.

If every `epsilon` is zero, equation (4.1) reduces independently to `alpha_j<=1`, giving family B. ∎

Every factor with `alpha_i=0` lies in the equality locus

\[
\dim(E_2\cap\operatorname{Sym}^2L_i)=3
\]

and is governed by the disjoint-support `2 x 3` / `3 x 2` classification. In family A, at least three factors are extremal. In family B, the number of extremal factors ranges from zero to four.

## 5. Current frontier

After the endpoint exclusion, the exact fixed-four state set is

\[
20\le b\le26,
\qquad
0\le d\le b-20.
\]

It contains 28 states:

```text
3 states:  already strict with Gamma>=0
5 states:  close if p<=23
5 states:  close if p<=59
15 states: structural exclusion or a stronger invariant required
```

The maximum remaining quotient-gain requirement is 157.

## 6. Next target

The next minimal task is the `b=26` near-equality classification.

1. Treat the eight family-A patterns by coupling one 14-dimensional quadratic derivative space to three extremal spaces.
2. Treat the sixteen family-B patterns by the number and support types of the factors with `alpha_i=0`.
3. Derive the analogue of common-quotient rigidity with one unit of projection defect.

No SAT layer, Hilbert-scheme registry, or general workflow abstraction is justified before these 24 patterns are reduced algebraically.

## 7. Reproduction

Run

```bash
python scripts/n6_fixed_four_coupled_frontier.py
python scripts/n6_extremal_six_plane_audit.py
```

The first output records the raw 36-state range, the eight excluded `b=27` states, and the current 28-state range. The second replays the exact local equality-plane certificate. Modular ranks are used only with matching characteristic-zero upper bounds.
