# `perm_7` v6 integration audit and promotion gate

## Current decision

```text
L50-AUDIT-PASS
L50-FINDINGS-LEDGER
MINORS-REPAIRED
SELECTIVE-IMPORT-COMPLETE
ACTIVE-HISTORY-RECONCILED
L50-MINIMAL-DEPENDENCY-GRAPH
L50-SCOPE-FROZEN
L50-PRE-CI-GATE-PASS
L50-EVIDENCE-GATE-PASS
L50-INTEGRATED-AND-PROMOTED
V5-FALLBACK-DORMANT
LOWER51-GATE-OPEN
```

This note records the completed integration decision.  GitHub Actions run
`32582021981` succeeded at proof-and-evidence head `4f93d26`, so the repository
interval is promoted to `50..64`.  A local test result, a running workflow, or
a successful workflow at another commit was not used as the receipt.

## Findings ledger

| severity | location | finding | repair and impact |
|---|---|---|---|
| minor | endpoint B | The arbitrary five-dimensional quotient argument was compressed to an unsupported orientation-independent sentence. | Replaced by the characteristic-zero Boolean quotient proof that `A/(W)` has zero degree four.  The endpoint is now independent of coordinate orientation. |
| minor | local equality spectrum | The rank-five full-quotient equality type `u=15` was omitted from the prose classification. | Added it and proved it cannot participate in a 49-term equality chain. |
| minor | finite evidence | Two load-bearing replay families were named only under an unpublished generation result path. | Imported the primary and independent rank-six profile and slope-table sources with their frozen payloads. |
| editorial | proof references | The candidate used generation-private replay paths. | Replaced them with repository-local paths and commands. |

No fatal or major finding remains.  The mathematical audit verdict is
`AUDIT-PASS-WITH-MINOR-REPAIRS`.  The repairs are local and do not assume the
conclusion of lower 50.

## Minimal theorem dependency graph

```text
Shafiei quadratic generation
        |
        +--> E2^(1)=E3 and E3^(1)=E4
        |
Bukh compression + two exact Ferrers DPs
        |
        +--> C6(47)=37 and C6(48)=44
                    |
                    +--> factor-span floors 5 and 12
                              |
small permanent intersections + exact local symbols
                              |
                              +--> universal slope ten
                                        |
rectangular Sylvester upper bound + global filtration lower bound
                                        |
                                        +--> exhaustive 49-term packets A/B
                                                  |
correct degree-3/4 direct-basis projection
                  |                               |
                  +--> endpoint A contradiction  |
                  +--> endpoint B contradiction <-+
                              |
                              +--> no 49-term identity
```

The exact finite computations are restricted to the two section-cap dynamic
programs, rank-six normal-form profiles, and coordinate local-symbol tables.
The arbitrary-orientation modular search and the exhaustive `F_2` Boolean
calculation are falsifiers only; the characteristic-zero arguments carry the
theorem.

## Active-history reconciliation

The active v4/v5 results are preserved.  The common-graph theorem and the
Packet-A/Packet-B modules remain independent structural information.  Their
packet-specific closure program becomes a dormant fallback after lower 50 is
promoted because the direct middle-degree proof bypasses it; none of those
files is deleted or reinterpreted as evidence for the shorter proof.  False
quadratic-surjectivity and automatic plain-induction routes remain rejected
controls.

The selective import contains one theorem-facing proof, three load-bearing
replay families, independent implementations, compact frozen data, negative
proof-contract tests, an evidence manifest, and the two reconstruction audit
notes.  It does not import unrelated exact-64 experiments.

## Scope and signoff boundary

The audited claim is only

```text
ordinary ChowRank(perm_7) >= 50
over an algebraically closed field of characteristic zero.
```

All summands are arbitrary nonzero products of seven linear forms; repeated
or dependent factors are allowed.  No border-rank, positive-characteristic,
exact-rank-64, or general-`n` conclusion is made.

The proof-and-evidence receipt is complete.  This promotion commit updates the
public status, marks v5 packet-specific closure work as a dormant fallback,
and opens v6 Modules 09--12.  It must receive its own successful exact-head CI
receipt before final signoff; the workflow result itself supplies that
head-to-receipt binding without requiring a self-referential metadata commit.
