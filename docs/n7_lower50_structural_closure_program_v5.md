# `perm_7` lower-50 structural closure program v5

## Status and claim boundary

`DORMANT FALLBACK PROGRAM — LOWER 50 WAS PROMOTED BY V6.`

Created: 2026-08-22  
Frozen mathematical input HEAD: `508dd5594a7f97ed830a5acf42ba47aa45ad7e6a`  
Active research PR: `#31`  
Active branch: `agent/general-column-sign-rank`

This program is retained as the packet-specific fallback to
`docs/n7_lower50_decisive_execution_program_v4.md`.  The v6 direct
middle-degree proof bypassed these closure tasks and promoted lower 50; the
modules below remain independent structural research rather than active
promotion gates.

The current ordinary characteristic-zero interval remains

\[
\boxed{50\leq\operatorname{ChowRank}(\operatorname{perm}_7)\leq64}.
\]

The sole promotion target of this program is

\[
\boxed{\operatorname{ChowRank}(\operatorname{perm}_7)\geq50}.
\]

Lower 51, lower 52, exact rank 64, border rank, positive characteristic, and a
general formula for `perm_n` remain dormant until lower 50 is frozen and
audited.

## Completed v4 boundary

The following are terminal v4 inputs.

1. **Weighted common-graph Packet B is closed.**  The five surviving Hilbert
   frontiers were eliminated through exact Waring-replacement arguments:
   the complete `q5=2`, `q5=3`, and `q5=4` classes are impossible.  This is
   `B1-CLOSED`.
2. **Arbitrary Packet B remains open.**  Its endpoint condition is the global
   extension
   \[
   \ker B\subseteq\operatorname{im}C,
   \qquad BC=C_{3,4}(\operatorname{perm}_7),
   \]
   equivalently the surjectivity of the cubic-apolar evaluation
   \[
   I_3(\operatorname{perm}_7)\twoheadrightarrow\ker B.
   \]
   Abstract exactness does not recover the 49 term summands.
3. The target torus does not automatically lift to the middle extension.
   A row-torus local-identifiability theorem would recover monomial quotient
   frames and row-block graph support, while projective-tail synchronization
   remains a separate rank-one-minor theorem.
4. Exact low-layer Packet-B survivors rule out local shortcuts but are not
   complete 42-complement packets.  Canonical two-transposition joins have
   positive defects, generic fifth terms do not repair them, and the smallest
   gap-aligned rank-one chart is empty.
5. **Packet A remains open.**  The all-Hessian-witness-zero branch and the
   `K2=0` branch are closed.  Every surviving Packet-A point has
   `W^all != 0`, `K2 != 0`, and `K5 != 0`.
6. The plain labelled complementary `2/5` and `3/4` kernel-image conditions are
   automatic after target containment and endpoint rank equality.  Repeating
   either interface is prohibited.  A new Koszul, recursive-Koszul, Pluecker,
   cross-weight, or second-order component is required.
7. No exact complete Packet-A or Packet-B endpoint survivor is known.  No
   lower-50 promotion has occurred.

## Program architecture

The program has eleven theorem workstreams and 154 numbered tasks.

| Module | Tasks | Purpose |
|---|---:|---|
| `00_v4_terminal_handoff.md` | 10 | freeze terminal results and reject superseded routes |
| `01_b2_multidegree_term_recovery.md` | 18 | recover term summands from a multi-degree Chow module |
| `02_b2_chow_fiber_identifiability.md` | 16 | prove row-torus local identifiability or freeze a survivor |
| `03_b2_tail_moment_rigidity.md` | 18 | force rank-one projective tails after row splitting |
| `04_b2_survivor_completion_and_boundaries.md` | 12 | decide the remaining exact slice and join branches |
| `05_packet_a_refined_koszul_program.md` | 18 | build a nonautomatic refined representation gate |
| `06_packet_a_cross_block_plucker.md` | 16 | use nonlinear compatibility across seven target blocks |
| `07_packet_a_second_order_geometry.md` | 12 | attack the special secant point beyond tangent equations |
| `08_universal_bypass_invariants.md` | 14 | test a small exact shortlist that could prove lower 50 directly |
| `09_cross_packet_decision_logic.md` | 8 | combine or separate the two endpoint packets correctly |
| `10_evidence_audit_promotion.md` | 12 | characteristic-zero evidence, adversarial audit, and promotion |

The execution waves and stop rules are in
`docs/n7_lower50_v5/11_execution_waves_and_stop_rules.md`.

## Priority allocation

Until one endpoint packet is closed:

```text
35%  arbitrary Packet-B multi-degree term recovery and identifiability
20%  Packet-B projective-tail moment rigidity
25%  Packet-A refined Koszul / Pluecker / cross-weight obstruction
10%  universal bypass invariant shortlist
10%  exact replay, survivor verification, and adversarial review
```

This is a research priority guide, not a scheduling or infrastructure layer.

## Required next replanning threshold

Do not replan after another interface note or bounded negative sample.  The next
replanning cycle is allowed only after at least one of the following:

```text
B2-FRAMES-AND-BLOCKS-RECOVERED
B2-TAILS-SYNCHRONIZED
B2-CLOSED
A-REFINED-OPERATOR-HAS-POSITIVE-ENDPOINT-DEFECT
A-CLOSED
UNIVERSAL-FLATTENING-PROVES-LOWER-50
EXACT-COMPLETE-49-TERM-ENDPOINT-SURVIVOR
LOWER-50-PROMOTED
```

## Explicitly stopped routes

The following do not count as progress and are not authorized:

- rerunning the closed common-graph Hilbert frontiers;
- enlarging monomial-curve, sign, shear, or random finite-field families
  without a theorem-defined component;
- rebuilding the plain Packet-A `2/5` or `3/4` Sylvester inclusion;
- inferring termwise splitting from the abstract middle exact sequence;
- inferring projective-tail equality from row-torus eigenlines alone;
- scanning unrestricted `GL_6` or 49-term factor coordinates;
- starting lower 51 or lower 52 before lower 50 is frozen;
- adding a scheduler, database, experiment manager, registry, or generalized
  computer-algebra framework.
