# Ledger delta: coordinate second-order matching-envelope barrier

Add the following route diagnostic after the coordinate regular first-order
closure theorem.

For every support of at most six cells,

```text
E2(A) = {perfect matchings M : |M cap A| >= 2}
|E2(A)| <= 14.
```

The 96 equality supports form one row-column orbit and are exactly `C6` graphs
on a selected `3 x 3` subblock. Six equality envelopes from one fixed `3 x 3`
block cover all 24 perfect matchings.

Consequences:

```text
raw coordinate second-order support counting = CLOSED ROUTE
coefficient-valued second-order system        = OPEN
coordinate second-order witness               = NOT CONSTRUCTED
mu(6,4)                                        = OPEN in [6,8]
unrestricted Chow-rank improvement             = false
border-rank improvement                        = false
```
