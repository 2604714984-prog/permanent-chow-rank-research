# Ledger delta: quartic four-block zero

Add the following stacked result:

| ID | Status | Statement | Evidence |
|---|---|---|---|
| `G-QUARTIC-FOUR-BLOCK` | `PROOF_DRAFT_COMPLETE`, `COMPUTATION_REPLAYED` | Every four degree-six Chow terms have zero literal intersection with `D_4(perm_6)`; hence `mu(6,4)>=5`. | `docs/general_quartic_four_block_zero.md`, primary and independent exact replays |

Supersede `(4,6,4) OPEN` by `(4,6,4) ZERO`. Keep `(4,8,3)` open. This changes no unrestricted Chow-rank or border-rank statement.
