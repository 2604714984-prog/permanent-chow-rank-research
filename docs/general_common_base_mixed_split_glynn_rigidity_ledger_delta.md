# Ledger delta: common-base mixed-split Glynn rigidity

For a fixed deleted sign, allow every compressed atom to choose an arbitrary
`m-2` / two-column split. The exact threshold remains

```text
2^(m-1)-1.
```

At equality all nonbase signs occur once and the highest defect layer forces
all atoms to use one common split. For `m=4`:

```text
atoms per fixed-base dictionary        42
assignments of splits checked      279,936
minimal block count                     7
minimal formulas per base               6
minimal formulas across all bases      48
```

```text
core: b060620eec6f6a4dc016024ffec05230494b280af9275e8b4693be3a042ff93b
status: STRICT_DICTIONARY_RIGIDITY_THEOREM
both base and split varying: OPEN
mu(6,4): OPEN IN [6,7]
```
