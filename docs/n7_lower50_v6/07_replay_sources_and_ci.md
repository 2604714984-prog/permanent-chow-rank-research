# v6 Module 07 — exact replay, sources, and CI

No theorem promotion is based only on prose or on the Rethlas publication
replay, which explicitly covers subclaims rather than the complete proof.

## EV-01 — select the minimal replay set

Identify only the finite computations that are load-bearing for lower 50: section caps, rank-six profiles, and slope-ten coordinate tables.

**Decisive output:** `LOAD-BEARING-REPLAY-LIST`.

## EV-02 — port section-cap replay

Run both independent dynamic programs on the active branch and freeze all `C_d(q)` values.

**Decisive output:** `SECTION-REPLAY-PASS`.

## EV-03 — port rank-six profile replay

Recompute the normal-form apolar profiles over rationals and at two good primes.

**Decisive output:** `RANK6-REPLAY-PASS`.

## EV-04 — port slope table replay

Regenerate all coordinate symbol minima and equality witnesses using two independently written implementations.

**Decisive output:** `SLOPE-REPLAY-PASS`.

## EV-05 — verify finite-field direction

For each modular minor, state whether it supplies a characteristic-zero lower bound, equality via an independent upper bound, or diagnostic evidence only.

**Decisive output:** `MODULAR-DIRECTION-AUDIT`.

## EV-06 — source-check Shafiei

Read the cited theorem and verify quadratic generation, field hypotheses, and permanent sign conventions.

**Decisive output:** `SHAFIEI-VERIFIED`.

## EV-07 — source-check Bukh

Read the theorem and compression lemmas and verify the finite consequence used by the proof.

**Decisive output:** `BUKH-VERIFIED`.

## EV-08 — hash proof and evidence

Freeze the audited proof text, scripts, JSON, and external-source identifiers in one compact manifest.

**Decisive output:** `L50-EVIDENCE-MANIFEST`.

## EV-09 — add theorem-facing tests

Add focused tests that fail under reversed apolar degrees, wrong transpose orientation, wrong semicontinuity direction, or omission of arbitrary quotient cases.

**Decisive output:** `L50-NEGATIVE-REGRESSIONS`.

## EV-10 — run a fresh independent audit

A reviewer not following the candidate proof order reconstructs the chain and records fatal/major/minor findings.

**Decisive output:** `FRESH-L50-AUDIT`.

## EV-11 — obtain an exact CI receipt

Run English-only scan, focused tests, independent replays, and the configured repository suite at one exact active-branch commit.

**Decisive output:** `L50-CI-RECEIPT`.

## EV-12 — CI decision

A missing or running workflow is not pass. Promotion requires a completed success receipt tied to the exact theorem head.

**Decisive output:** `L50-EVIDENCE-GATE-PASS`.
