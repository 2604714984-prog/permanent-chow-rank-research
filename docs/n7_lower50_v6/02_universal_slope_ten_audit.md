# v6 Module 02 — universal slope-ten lemma audit

Audit the local inequality for every product of seven linear forms, every
actual factor rank, every quotient orientation, and every permitted
`D_2(T) cap E_2` subspace. Equality types must be exact.

## LS-01 — audit `D3(T) cap E3=0`

Reprove the essential-variable argument for arbitrary nonzero elements of `E3`, including cancellation and restriction to one chosen 3-by-3 block.

**Decisive output:** `CUBIC-INTERSECTION-ZERO`.

## LS-02 — audit the quadratic intersection cap

Prove `dim(D2(T) cap E2)<=3` by a complete seven-edge bipartite four-cycle classification and verify the degeneration/collective-derivative-span direction.

**Decisive output:** `QUADRATIC-INTERSECTION-CAP`.

## LS-03 — classify rank-six normal forms

Reprove the six support normal forms `T_s` and verify there are no missing coefficient or zero-support strata.

**Decisive output:** `RANK6-NORMAL-FORMS`.

## LS-04 — replay actual derivative profiles

Recompute every apolar Hilbert profile for `s=1..6` by exact rational elimination and an independent modular method.

**Decisive output:** `RANK6-PROFILES-PASS`.

## LS-05 — freeze local symbol orientations

Define `beta+` and `beta-` from actual derivative spaces, quotient labels, and `F/R_T`. Add transpose-orientation negative controls.

**Decisive output:** `LOCAL-SYMBOLS-FROZEN`.

## LS-06 — audit the rank-six degeneration

Prove that the limit of actual derivative spaces contains the special monomial derivative spaces and that carrying quotient kernels and `R_T` through Grassmannians yields lower bounds in the claimed direction.

**Decisive output:** `RANK6-DEGENERATION-PROVED`.

## LS-07 — independently enumerate coordinate rows

Regenerate the complete coordinate tables behind (S7)–(S9), retain minimizers, and compare against the candidate payload.

**Decisive output:** `SLOPE-TABLES-INDEPENDENT-PASS`.

## LS-08 — audit rank-six `d=5`

Verify the plus-kernel bound, the independent minus-rank floor, and the strict margin without importing a coordinate-only conclusion.

**Decisive output:** `RANK6-D5-STRICT`.

## LS-09 — audit rank-six `d=6`

Prove both full symbols injective and equality occurs exactly for `s=1,2`.

**Decisive output:** `RANK6-D6-EQUALITY`.

## LS-10 — audit rank-seven rows

Verify arbitrary quotient orientations for the squarefree monomial, the `d=7` injectivity correction from 69 to 70, and the absence of other equality rows.

**Decisive output:** `RANK7-EQUALITY-CLASSIFIED`.

## LS-11 — audit rank-five degeneration types

Prove the two exponent partitions are exhaustive and validate every kernel estimate and full-quotient injectivity claim.

**Decisive output:** `RANK5-ROWS-PROVED`.

## LS-12 — audit factor ranks one through four

Replace compressed prose with explicit kernel-dimension lemmas for each rank and quotient dimension.

**Decisive output:** `LOW-RANK-ROWS-PROVED`.

## LS-13 — freeze the equality spectrum

Produce one machine-readable list of all positive-increment equality types and all strict margins.

**Decisive output:** `SLOPE-EQUALITY-SPECTRUM`.

## LS-14 — adversarial arbitrary-quotient search

Use exact or modular searches only as falsifiers for omitted quotient orientations and `R_T` placements. A pass is diagnostic, not proof.

**Decisive output:** `SLOPE-ADVERSARIAL-NO-COUNTEREXAMPLE`.

## LS-15 — independent semicontinuity review

A fresh reviewer must annotate every specialization with the generic/special rank direction and reject any unsupported preservation claim.

**Decisive output:** `SEMICONTINUITY-AUDIT-PASS`.

## LS-16 — slope-ten audit decision

Return pass only after all factor ranks, equality types, exact tables, and semicontinuity steps are closed.

**Decisive output:** `SLOPE-TEN-AUDIT-PASS`.
