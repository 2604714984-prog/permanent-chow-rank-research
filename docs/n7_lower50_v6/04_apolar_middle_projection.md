# v6 Module 04 — corrected apolar middle projection theorem

This is the new bridge absent from the active v5 frontier. It must be audited
independently because both endpoint contradictions depend on it.

## MP-01 — freeze `C`, `R`, and restriction maps

Write all degreewise maps and pairings explicitly. Distinguish the apolar ideal `I`, intersection ideal `J`, quotient algebra `C`, and ideal `R=I/J`.

**Decisive output:** `MIDDLE-NOTATION-FROZEN`.

## MP-02 — prove corrected graded duality

Reprove the single-term and pair cokernel dualities with degrees two/five and three/four in the correct orientation.

**Decisive output:** `GRADED-DUALITY-PROVED`.

## MP-03 — retain quadratic counterexamples

Replay the one-term and disjoint-pair counterexamples to local quadratic surjectivity. They must fail any accidentally reintroduced false lemma.

**Decisive output:** `QUADRATIC-NEGATIVE-CONTROLS`.

## MP-04 — prove `R` is multiplicatively stable

Show that multiplying an element of `R_d` by a degree-one codeword lands in `R_{d+1}` in the componentwise local-algebra model.

**Decisive output:** `R-IDEAL-MULTIPLICATION`.

## MP-05 — construct basis-supported codewords

Given a direct factor decomposition of `V`, prove the existence and exact restriction pattern of linear codewords supported on one basis block.

**Decisive output:** `BASIS-CODEWORDS`.

## MP-06 — audit block-triangular quotient symbols

Order direct basis terms first and write the two global quotient-polarization matrices with explicit diagonal blocks.

**Decisive output:** `BLOCK-TRIANGULAR-FORM`.

## MP-07 — prove full minus injectivity

For every basis term, prove a killed cubic lies in `E3` and hence vanishes by the small-intersection lemma.

**Decisive output:** `FULL-MINUS-INJECTIVE`.

## MP-08 — prove full plus injectivity

Verify full polarization and reduction modulo `E3` remain injective on each actual local degree-four space.

**Decisive output:** `FULL-PLUS-INJECTIVE`.

## MP-09 — prove basis restriction surjectivity

Use correctly graded duality and diagonal injectivity to prove `R3` and `R4` surject onto all direct basis blocks.

**Decisive output:** `DIRECT-BASIS-SURJECTIVITY`.

## MP-10 — derive the 245 cap

Prove the sum of local middle dimensions of any direct basis packet is at most 245, with all global defect terms retained.

**Decisive output:** `DIRECT-BASIS-MIDDLE-CAP`.

## MP-11 — prove equality gives isomorphisms

When the cap is attained, prove both degree-three and degree-four restriction maps are isomorphisms, not merely surjections.

**Decisive output:** `DIRECT-BASIS-ISOMORPHISMS`.

## MP-12 — reconcile active-v4 automaticity results

Explain precisely why the active branch's automatic plain `2/5` and `3/4` induction maps do not imply or contradict this basis-supported algebra-multiplication theorem.

**Decisive output:** `NO-CONTRADICTION-WITH-V4`.

## MP-13 — audit dimensions in both endpoints

Independently derive 245 for endpoint A and 210 for the endpoint-B direct basis, together with equality in the relevant global bounds.

**Decisive output:** `ENDPOINT-BASIS-DIMENSIONS`.

## MP-14 — fresh proof without candidate order

A separate reviewer reconstructs the theorem starting from the dual cokernel formula rather than following the blueprint's sequence.

**Decisive output:** `MIDDLE-PROJECTION-INDEPENDENT-PROOF`.

## MP-15 — finite algebra controls

Build small Boolean and rank-six local-algebra examples verifying block support, multiplication, and the surjective/isomorphic distinction.

**Decisive output:** `MIDDLE-PROJECTION-CONTROLS`.

## MP-16 — middle projection audit decision

No endpoint audit begins until this module has no fatal or major finding.

**Decisive output:** `MIDDLE-PROJECTION-AUDIT-PASS`.
