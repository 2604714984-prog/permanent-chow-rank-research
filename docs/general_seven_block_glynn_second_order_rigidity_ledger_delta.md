# Ledger delta: second-order local rigidity of the compressed Glynn witness

Add the following strict local route theorem to the active `(6,4)` frontier:

- for every deleted standard summand, the other six full tangent maps have
  rank 574 and a complete 92-dimensional characteristic-zero kernel;
- the projected tangent sum has dimension 108;
- all 4278 polarized kernel pairs have curvature contained in that projected
  tangent sum;
- the curvature span has dimension 24 but quotient rank zero;
- the missing summand remains outside the projected tangent sum;
- therefore the known seven-block witness cannot be compressed to six by a
  regular first- or second-order local deformation.

Frozen core:

```text
e80c3b30e9df09144eef28f3424d0b4e44b0f3e6a737e12ef0a8e4a6d5f84a4c
```

The unrestricted interval remains

```text
6 <= mu(6,4) <= 7.
```

Remote, singular, and third- or higher-order six-block mechanisms remain open.
