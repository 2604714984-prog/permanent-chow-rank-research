# Ledger delta: canonical `C6` second-order pair cancellation

Add the following restricted zero theorem after the canonical six-`C6` source
reduction.

For the six frames `A_mu=K_(3,3)\graph(mu)` in one fixed `3 x 3` block, every
order-zero source relation is a sum of nine pairwise cross-source differences.
The vanishing first derivative forces the boundary factor velocities of the
two endpoint frames to agree on every active shared source. Their opposite
order-zero coefficients then cancel every quadratic two-boundary term.

Consequently all eighteen second-order perfect-matching coefficients not
containing `(3,3)` are zero. The canonical six-envelope cover cannot lift to a
second-order permanent target.

Strict status:

```text
canonical fixed-3x3 six-C6 second-order cover = ZERO
general coordinate second-order covers         = OPEN
coordinate higher-order lifts                   = OPEN
mu(6,4)                                         = OPEN in [6,8]
unrestricted Chow-rank improvement              = false
border-rank improvement                         = false
```
