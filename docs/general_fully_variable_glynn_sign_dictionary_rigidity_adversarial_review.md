# Adversarial review: corrected fully variable sign dictionary

## Why was the previous theorem withdrawn?

Its Python replay hard-coded sixteen six-star projected candidates instead of
classifying all projected supports.  The independent C++ scanner correctly
found a four-direction representation and failed because its expected value was
six.  The CI failure therefore exposed a mathematical premise error, not an
environmental problem.

## Is the corrected minimum four exact over characteristic zero?

Yes.  The scan uses the prime `2^61-1`.  Entries have absolute value at most 32
and the relevant minors have at most five columns, so their Hadamard bound is
far smaller than the prime.  A nonzero rational minor cannot disappear modulo
that prime.

## Do the sixteen four-direction states produce a witness?

No.  The unique projected coefficients are restored to every possible base and
column-split label.  All `16*18^2*6^2=186,624` full 256-coordinate tensor
assignments fail exactly over the integers.

## Does this repair prove threshold seven?

No.  Five atoms are excluded by the inherited five-block zero theorem, but the
projected five- and six-direction states are not classified by this packet.
The fully variable sign threshold is open in `[6,7]`.

## Does the correction affect the seven-block construction?

No.  The explicit one-term Glynn compression remains a valid seven-block
witness.  Fixed-split variable-base rigidity and common-base mixed-split
rigidity are separate theorems and are not invalidated.

## Strict conclusion

The superseded threshold-seven statement is retracted.  The corrected theorem
closes only the minimal four-direction projected stratum and records the exact
remaining boundary.
