# Adversarial review: common-base mixed-split Glynn rigidity

## Does the quotient erase too much?

It intentionally erases the base-tail term and therefore only proves the lower
bound. Equality is handled separately by the highest surviving defect layer,
where different column splits occupy distinct multigraded components.

## Why must a split group be empty or full?

The degree-`m-2` quotient tensors on nonzero Boolean points have exactly one
relation, the full-support Walsh relation. A proper nonempty subcollection with
the prescribed Glynn coefficients cannot vanish.

## Is the quartic equality classification merely numerical?

No. The general unique-relation proof forces a uniform split. The complete
279,936-assignment scan is an exact independent replay of the same conclusion.

## Does this combine with variable-base fixed-split rigidity to close all sign atoms?

Not automatically. One theorem fixes the split and varies bases; this theorem
fixes the base and varies splits. A representation in which both vary
simultaneously is not covered by either argument.

## Strict conclusion

The theorem closes the common-base mixed-split dictionary, not arbitrary
six-block Chow derivatives. The unrestricted interval remains `[6,7]`.
