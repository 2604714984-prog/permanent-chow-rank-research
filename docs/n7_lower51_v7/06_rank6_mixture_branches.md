# v7 Module 06 — rank-six mixture branches

Classify and close every 50-term packet containing factor-rank-six summands.

## R6-01 — enumerate rank-six counts and support types

Use the exact atom catalog to list every permitted number of `s=1..6` terms.

**Decisive output:** `R6-COUNT-TABLE`.

## R6-02 — separate equality and surplus types

Treat `s=1,2` equality terms separately from `s=3..6` near-equality terms.

**Decisive output:** `R6-TYPE-SPLIT`.

## R6-03 — classify mixed direct bases

Enumerate all rank-six/rank-seven dimension compositions totaling 49.

**Decisive output:** `R6-MIXED-BASES`.

## R6-04 — classify no-basis mixed profiles

Use all-order budget and subset floors to list all mixed profiles without a direct basis.

**Decisive output:** `R6-NOBASIS-PROFILES`.

## R6-05 — freeze local rank-six algebras

Record degree-two through degree-five multiplication tables for every surviving normal form.

**Decisive output:** `R6-LOCAL-ALGEBRAS`.

## R6-06 — derive mixed basis projection

Generalize the corrected degree-three/four direct-basis theorem to each mixed basis total.

**Decisive output:** `R6-BASIS-PROJECTION`.

## R6-07 — compute residual kernel budgets

Determine `(K3,K4)` allowances after every mixed basis restriction.

**Decisive output:** `R6-RESIDUAL-BUDGETS`.

## R6-08 — recover quotient frames

Use multi-degree term recovery to identify the six-dimensional local frames from the aggregate extension.

**Decisive output:** `R6-FRAME-RECOVERY`.

## R6-09 — classify graph-complement terms

When rank-seven terms complement a rank-six direct sum, derive the exact graph-map rank and overlap conditions.

**Decisive output:** `R6-GRAPH-BRANCHES`.

## R6-10 — synchronize projective tails

Prove or falsify the rank-one-minor theorem needed to align the graph tails.

**Decisive output:** `R6-TAIL-RIGIDITY`.

## R6-11 — handle noncanonical joins

Use the existing one- and two-transposition survivors as negative controls, not endpoint survivors.

**Decisive output:** `R6-JOIN-BOUNDARIES`.

## R6-12 — handle higher-support rank-six terms

Exploit their forced positive surplus and middle dimension to isolate a smaller residual module.

**Decisive output:** `R6-HIGH-SUPPORT-RESULT`.

## R6-13 — combine permanent target blocks

Enforce all relevant missing-row and missing-column target blocks with one coefficient system.

**Decisive output:** `R6-TARGET-COMPATIBILITY`.

## R6-14 — test apolar syzygies

Use low-degree permanent syzygies only with term labels and correct quotient orientation.

**Decisive output:** `R6-SYZYGY-RESULT`.

## R6-15 — test refined Koszul components

Choose a nonautomatic representation component whose one-term cap distinguishes the mixed branch.

**Decisive output:** `R6-KOSZUL-RESULT`.

## R6-16 — test cross-block Pluecker equations

Use nonlinear factor-plane compatibility on any linear-module survivor.

**Decisive output:** `R6-PLUECKER-RESULT`.

## R6-17 — cover rank drops and boundaries

Handle coincident graph maps, zero minors, repeated factors, and flat limits.

**Decisive output:** `R6-BOUNDARY-COVERAGE`.

## R6-18 — freeze exact branch survivors

Serialize complete factors, coefficients, ranks, and identity residuals.

**Decisive output:** `R6-SURVIVOR-PAYLOAD`.

## R6-19 — decide every mixed branch

No rank-six packet remains described only by an experimental family.

**Decisive output:** `R6-BRANCHES-DECIDED`.

## R6-20 — issue the lane verdict

Return `RANK6-MIXTURES-CLOSED` or one independently verified full identity.

**Decisive output:** `R6-VERDICT`.
