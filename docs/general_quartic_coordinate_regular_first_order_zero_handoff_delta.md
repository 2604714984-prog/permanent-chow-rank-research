# Handoff delta: coordinate regular first-order closure

## Result

For six coordinate-initial degree-six Chow components in a regular
one-parameter family, an order-zero cancellation cannot have first derivative
with all 24 `perm_4` matching coordinates nonzero.

The one-frame first-order matching envelope has size at most six. After
including source-vector motion and every motion of the internal factor-label
kernel, the one-frame private matching capacity is at most two. Every
remaining target matching must lie in at least two component envelopes. The
resulting incidence squeeze forces the impossible local state

```text
envelope size = 6
private capacity = 2.
```

All 288 envelope-six frames instead have private capacity zero.

## Next task

Analyze coordinate-initial families whose order-zero and order-one totals both
vanish and whose first possible `perm_4` matching projection occurs at order
two. Do not broaden to a generic nonlinear solver before the polarized
second-order kernel interface is written explicitly.

## Claim boundary

```text
coordinate regular first-order six-block = ZERO
coordinate second-order leading          = OPEN
noncoordinate / singular                 = OPEN
mu(6,4)                                  = OPEN in [6,8]
```
