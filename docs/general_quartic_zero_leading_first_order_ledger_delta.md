# Ledger delta: zero-leading coordinate first-order reduction

Add the following restricted route result to the active `perm_6` quartic frontier:

- one zero-leading coordinate component: first-order excluded;
- two zero-leading coordinate components: first-order excluded;
- three zero-leading coordinate components: first-order excluded;
- five zero-leading coordinate components: impossible;
- four zero-leading components: finite support survivor;
- six zero-leading components: open.

The unrestricted interval remains `6 <= mu(6,4) <= 8` and `28 <= ChowRank(perm_6) <= 32`.
