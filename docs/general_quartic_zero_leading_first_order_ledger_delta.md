# Ledger delta: zero-leading coordinate first-order envelope

Add the following restricted route result to the active `perm_6` quartic frontier:

- every coordinate six-frame has regular first-order matching envelope of size at most six;
- equality occurs for exactly 288 six-cell supports, in two row-column orbits of size 144;
- every equality support is a `P5 disjoint-union P3` bipartite graph with row and column degree sequence `(2,2,1,1)`;
- four equality envelopes can partition all 24 perfect matchings;
- no reduction by the number of zero-leading components is proved by support counting alone.

A former `6+5z` multi-component inference is explicitly retracted because positive-leading components can gain new matching coordinates at first order through nonmatching source coefficients.

The unrestricted interval remains `6 <= mu(6,4) <= 8` and `28 <= ChowRank(perm_6) <= 32`.
